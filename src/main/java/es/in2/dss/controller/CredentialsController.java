package es.in2.dss.controller;

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/dss/v1/credentials")
@RequiredArgsConstructor
public class CredentialsController {

    // todo: Create the required endpoints for the Remote Signature Service API: credentials/list, credentials/info,
    //  credentials/authorize, credentials/extendTransaction and credentials/sendOTP.

}
